export enum DetectionMode {
    INTERVAL = "Interval",
    ONCE = "Once",
}

export enum DiffusionOption {
    MATCH_MOOD = "Match mood",
    OPPOSITE_MOOD = "Opposite of mood",
    AUTO = "Automatic",
}
